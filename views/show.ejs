<%- include('partials/header') %>

<%- include("partials/navbar") %>

<div class="form pt-5">
  <div class="card">
    <div class="card-body">
      <div class="mb-2">
          <label class="form-label">Password</label>
          <input type="text" class="copy_text form-control" value="<%= data %>" disabled>
      </div>
      <button class="btn btn-sm mt-1" onclick="copy()">Copy Password</button>
    </div>
  </div>
</div>

<script src="/js/main.js"></script>

<%- include('partials/footer') %>